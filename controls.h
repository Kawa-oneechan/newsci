#pragma once
#include "types.h"
#include "fonts.h"

///<summary>Draws the window defined in <paramref name="windowDef" /> and handles any user interactions with standard controls.</summary>
extern void DrawWindow(sol::table windowDef);
